# ── À ajouter dans docker-compose.yml, dans la section "services:" ──────────
#
#   mvc-jpa:
#     build:
#       context: ./mvc-jpa
#     container_name: mvc-jpa
#     profiles:
#       - mvc
#     ports:
#       - "8081:8081"
#     environment:
#       SERVER_PORT: 8081
#       SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/orders_db
#       SPRING_DATASOURCE_USERNAME: postgres
#       SPRING_DATASOURCE_PASSWORD: postgres
#       MOCK_SERVICE_BASE_URL: http://mock-service:9090
#     depends_on:
#       postgres:
#         condition: service_healthy
#       mock-service:
#         condition: service_started
#     mem_limit: 1536m
#     cpus: 2.0
#
# ── Lancement ────────────────────────────────────────────────────────────────
#   docker compose --profile mvc up --build -d
#
# ── Vérification ─────────────────────────────────────────────────────────────
#   docker compose ps
#   curl.exe "http://localhost:8081/actuator/health"
#   curl.exe "http://localhost:8081/products/1"
#   curl.exe "http://localhost:8081/external/recommendations/4?delayMs=300"
